import MyCalendarEdit from '../../components/pages/myCalendarEdit/MyCalendarEdit';
import BaseContainer from "./BaseContainer";

const mapStateToProps = (state) => {
    return {}
};

const mapDispatchToProps = (dispatch) => {
    return {}
};

const MyCalendarEditContainer = BaseContainer(mapStateToProps, mapDispatchToProps, MyCalendarEdit);

export default MyCalendarEditContainer;