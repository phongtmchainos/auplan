import EditPhone from '../../components/pages/telephoneEdit/TelephoneEdit';
import {connect} from 'react-redux';

const mapStateToProps =(state)=>{
    return{

    }
};

const mapDispatchToProps=(dispatch)=>{
    return{

    }
};

const EditPhoneContainer =connect(mapStateToProps, mapDispatchToProps)(EditPhone);

export default EditPhoneContainer;