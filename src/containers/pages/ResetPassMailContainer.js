import ResetPassMail from '../../components/pages/resetPassMail/ResetPassMail';
import {connect} from 'react-redux';

const mapStateToProps =(state)=>{
    return{

    }
};

const mapDispatchToProps=(dispatch)=>{
    return{

    }
};

const ResetPassMailContainer =connect(mapStateToProps, mapDispatchToProps)(ResetPassMail);

export default ResetPassMailContainer;