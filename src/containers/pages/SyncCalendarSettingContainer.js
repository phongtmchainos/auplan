import BaseContainer from "./BaseContainer";
import SyncCalendarSetting from "../../components/pages/syncCalendarSetting/SyncCalendarSetting";

const mapStateToProps = (state) => {
    return {}
};

const mapDispatchToProps = (dispatch) => {
    return {}
};

const SyncCalendarSettingContainer = BaseContainer(mapStateToProps, mapDispatchToProps, SyncCalendarSetting);

export default SyncCalendarSettingContainer;