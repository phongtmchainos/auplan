import CalendarNewEntry from '../../components/pages/calendarNewEntry/CalendarNewEntry';
import {connect} from 'react-redux';

const mapStateToProps =(state)=>{
    return{

    }
};

const mapDispatchToProps=(dispatch)=>{
    return{

    }
};

const CalendarNewEntryContainer =connect(mapStateToProps, mapDispatchToProps)(CalendarNewEntry);

export default CalendarNewEntryContainer;