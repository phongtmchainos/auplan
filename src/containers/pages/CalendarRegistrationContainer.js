import CalendarRegistration from '../../components/pages/calendarRegistration/CalendarRegistration';
import {connect} from 'react-redux';

const mapStateToProps =(state)=>{
    return{

    }
};

const mapDispatchToProps=(dispatch)=>{
    return{

    }
};

const CalendarRegistrationContainer =connect(mapStateToProps, mapDispatchToProps)(CalendarRegistration);

export default CalendarRegistrationContainer;