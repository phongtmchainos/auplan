import ScheduleNewEntry from '../../components/pages/scheduleDetail/ScheduleDetail';
import {connect} from 'react-redux';

const mapStateToProps =(state)=>{
    return{

    }
};

const mapDispatchToProps=(dispatch)=>{
    return{

    }
};

const ScheduleNewEntryContainer =connect(mapStateToProps, mapDispatchToProps)(ScheduleNewEntry);

export default ScheduleNewEntryContainer;