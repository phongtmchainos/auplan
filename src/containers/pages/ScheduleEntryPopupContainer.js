import ScheduleEntryPopup from '../../components/pages/scheduleEntryPopup/ScheduleEntryPopup';
import {connect} from 'react-redux';

const mapStateToProps =(state)=>{
    return{

    }
};

const mapDispatchToProps=(dispatch)=>{
    return{

    }
};

const ScheduleEntryPopupContainer =connect(mapStateToProps, mapDispatchToProps)(ScheduleEntryPopup);

export default ScheduleEntryPopupContainer;