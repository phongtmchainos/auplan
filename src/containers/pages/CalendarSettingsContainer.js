import CalendarSettings from '../../components/pages/calendarSettings/CalendarSettings';
import {connect} from 'react-redux';

const mapStateToProps =(state)=>{
    return{

    }
};

const mapDispatchToProps=(dispatch)=>{
    return{

    }
};

const CalendarSettingsContainer =connect(mapStateToProps, mapDispatchToProps)(CalendarSettings);

export default CalendarSettingsContainer;